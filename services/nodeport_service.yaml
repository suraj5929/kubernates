apiVersion: v1
kind: Service
metadata:
  name: my-web-app-service
spec:
  type: NodePort
  selector:
    app: my-web-app
  ports:
    - port: 80          # Service port inside cluster
      targetPort: 80    # Container port
      nodePort: 30770   # Port accessible on cluster nodes